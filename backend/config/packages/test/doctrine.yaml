# backend/config/packages/test/doctrine.yaml
doctrine:
    dbal:
        url: '%env(resolve:DATABASE_URL)%'
        dbname_suffix: '_test%env(default::TEST_TOKEN)%'
    orm:
        auto_generate_proxy_classes: true
