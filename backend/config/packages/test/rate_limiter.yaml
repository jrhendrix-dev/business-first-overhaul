framework:
    rate_limiter:
        enabled: true
        limiters:
            contact_form:
                policy: fixed_window
                limit: 999999
                interval: '1 minute'
                # use the built-in cache pool for rate limiter storage
                storage_service: 'cache.rate_limiter'
