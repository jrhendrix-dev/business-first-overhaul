framework:
    rate_limiter:
        enabled: true
        limiters:
            contact_form:
                policy: fixed_window
                limit: 999999          # effectively disable throttling in tests
                interval: '1 minute'
                storage_service: 'rate_limiter.storage.in_memory'
